# Overdrive Core - プロジェクト概要と開発コンテキスト

本プロジェクトは、フロム・ソフトウェアの『アーマード・コア (ARMORED CORE VI)』のような高速メカアクションゲームを、**C++ ＋ DirectX 11 ＋ SDL3** で自作し、将来的な **PCVR（Meta Quest 3S等での一人称コックピット視点 FPV）** に対応させるプロジェクトです。

---

## 1. 採択技術スタック
- **グラフィックス**: DirectX 11 (`d3d11.lib`, `dxgi.lib`, `d3dcompiler.lib`)
- **ウィンドウ / 入力**: SDL3 (FetchContent で自動ビルド、ゲームパッド完全対応)
- **ビルドシステム**: CMake (>= 3.20) + Ninja + MSVC (C++20)
- **物理エンジン（次期予定）**: Jolt Physics (マルチスレッド・仮想キャラクタコントローラー)
- **モデル読み込み（次期予定）**: fastgltf (glTF 2.0 / GLB)
- **3D音響（次期予定）**: miniaudio
- **VR対応（次期予定）**: OpenXR (`XR_KHR_D3D11_enable`) による FPV（一人称コックピット視点）＆ 通常ゲームパッド操作

---

## 2. これまでに実装済みの機能
1. **DirectX 11 レンダラー (`src/graphics/D3D11Renderer.cpp`)**
   - スワップチェーン、深度ステンシルバッファ (Zバッファ)、WVP定数バッファ
   - 3Dメカモデル（マゼンタ/ホワイトの自機、ダークチタン/クリムゾンレッドの敵機）の描画
   - 2D HUD（照準レティクル、自機APバー、リアルタイム ENゲージバー、左右弾薬バー、ネット状態）の描画
2. **サイバーグリッド床 ＆ 格納庫外壁 (`src/graphics/GridFloor.cpp`)**
   - AC6テストグラウンド風の青白く発光するグリッド線
   - 速度感・慣性を把握しやすい空間設計
3. **機体物理制御 ＆ ENシステム (`src/core/MechController.cpp`)**
   - 地上ホバー滑空（`Tab` / `B`ボタンで巡航ブースト切替）
   - クイックブースト QB（`Shift` / `X`ボタンで瞬間インパルスダッシュ、ロール傾き）
   - ジャンプ ＆ バーニア垂直上昇（`Space` / `A`ボタン長押しで推力浮遊、離すと高速重力落下）
   - アサルトブースト AB（`Ctrl` / `L3`で前傾超高速突進）
   - リアルタイム EN（エネルギー）消費・地上急速回復、自機AP（2500）管理と撃破後自動リスポーン
4. **デュアルカメラ (`src/graphics/Camera.cpp`)**
   - `V` キー / ビューボタンで **FPV（VRコックピット視点）** ↔ **TPS（三人称追従視点）** をトグル切替可能
5. **Jolt Physics 物理エンジン (`src/physics/PhysicsManager.cpp`)**
   - マルチスレッド・ジョブシステム（全CPUコア活用）
   - `CharacterVirtual` によるメカ機体の衝突判定・段差乗り越え・斜面登坂
   - 静的コライダー（地面、外壁、巨大支柱群、スロープ、高台プラットフォーム、障害物ブロック）
6. **武装・射撃システム (`src/combat/WeaponSystem.cpp`, `src/combat/TargetDummy.cpp`)**
   - 左右腕部ライフルの独立射撃（右クリック/RT、左クリック/LT）
   - 高速発光弾道（オレンジ＆シアンのトレーサービーム、初速 280m/s）
   - Jolt Physics のレイキャスト（`CastRay`）によるリアルタイム着弾・物理衝突判定
   - 練習用標的ターゲット 3体（地上、高台、空中ドローン、被弾点滅、耐久値、リスポーン）
7. **FCS ターゲットロックオンシステム (`src/combat/TargetLockSystem.cpp`)**
   - AC6風の二段階照準（外枠HUD ＋ 距離別速度追従のインナー赤照準）
   - ターゲットアシスト（ハードロックオン: 中クリック/R3/Fキーでカメラ・機体自動追尾、視線フリックでターゲット切替）
   - 敵機体（ダミーおよび敵プレイヤー機体）上の3Dホログラフィック照準枠、頭上HPバー、リアルタイム距離表示
8. **miniaudio 3D 空間音響 (`src/audio/AudioManager.cpp`)**
   - 左右射撃音（近接感のある高初速ビーム・実弾音）、爆発着弾音、ロックオン音、スラスターブースト音
   - VR頭部・カメラ位置追従リスナーによる立体音響定位
9. **OpenXR PCVR 完全統合 (`src/vr/VRManager.cpp`)**
   - Meta Quest 3S / PCVR HMD への左右両眼（Stereo）立体出力と6DoF頭部トラッキング
   - 3Dキャノピー立体HUD（コックピット前面2.2mに投影されるホログラフィック照準・ゲージ・距離計）
10. **WinSock2 4人対戦マルチプレイヤー（バトルロイヤル） (`src/network/NetworkManager.cpp`)**
    - ホストを中心とする高速低遅延UDPスター型トポロジー（最大4機 Free-For-All 同時対戦）
    - スロット自動割り当て（Player 1〜4: クリムゾン、コバルト、アンバー、エメラルド）
    - 4機体の初期スポーン位置（南、北、東、西）およびリスポーン制御
    - 全クライアント間の60Hz機体同期・パケットリレー、射撃・被弾・撃破判定
    - FCSターゲットアシストによる敵プレイヤー複数機体の個別ロック＆フリックターゲット切り替え
    - 相手機体ごとの独立カラーレンダリング、頭上HPバーおよび `PLAYER 1`〜`PLAYER 4` ネーム表示
    - 起動引数（`--host` / `--connect <IP>`）、設定ファイル（`network.ini`）、キー操作（`H`でホスト、`C`で接続）対応
11. **マガジン＆リロードシステム (`src/combat/WeaponSystem.cpp`, `src/graphics/D3D11Renderer.cpp`)**
    - 左右腕部ライフルの独立マガジン管理（装弾数 各30発、予備弾無限）
    - 弾切れ時の自動リロード ＆ 手動リロード（`R` キー / ゲームパッド十字下キー）
    - 少しゆっくりめのリロード時間（2.8秒）による立ち回り・駆け引きの創出
    - リロード中の琥珀色（ゴールド/アンバー）点滅チャージHUD表示（2Dデスクトップ ＆ VRキャノピー）
    - メカニカルラッチ＆電子チャイムによる手続き的リロード完了音（miniaudio）
12. **タイトルメニュー画面 ＆ 拡張可能メニューシステム (`src/ui/MenuSystem.cpp`, `src/graphics/D3D11Renderer.cpp`)**
    - ゲーム起動時のタイトルメニュー画面（シネマティック旋回カメラで佇む自機メカと格納庫背景）
    - 拡張可能なメニューシステム（`TRAINING MODE`、`MULTIPLAYER (HOST)`、`MULTIPLAYER (CONNECT)`、`EXIT GAME` 等、新項目を容易に追加可能）
    - 全アルファベット（A〜Z）・数字・記号を完全サポートしたサイバーベクターフォント描画
    - 選択カーソルパルス、ネオングロー枠、選択中項目の詳細説明文（Description）、操作ガイド表示
    - キーボード（`W`/`S`/`↑`/`↓`/`Enter`/`Space`）およびゲームパッド（十字キー、`A`決定、`B`終了）完全対応
    - ゲームプレイ中 `ESC` キーによるメインメニュー戻り対応
    - OpenXR VR HMDによる3Dコックピット前面ホログラム立体メニュー投影
13. **肩部ウェポンハンガー ＆ 4スロットモジュール式武器システム (`src/combat/WeaponSystem.cpp`, `src/graphics/D3D11Renderer.cpp`)**
    - 将来のガレージ・アセンブル（自由な武器パーツ換装）を見据えた完全モジュール化アーキテクチャ（`WeaponSlot`, `WeaponData`, `WeaponInstance`）
    - 4スロット管理（`RightArm`, `LeftArm`, `RightBack`, `LeftBack`）
    - 4種類の多彩な初期プリセット兵装（実弾アサルトライフル、パルスビーム、重プラズマ砲、バーストマシンガン）
    - アーマードコア（AC6）本家仕様のウェポンハンガー機能：
      - 通常は腕に持つ武器を肩ハンガーラックに背負って出撃可能
      - 左肩射撃キー（`Q` キー / ゲームパッド `LB`）で左腕と左肩の武器を瞬時にスワップ
      - 対称設計として右肩（`E` キー / ゲームパッド `RB`）でもスワップ可能
      - 各武器インスタンスが独立して残弾数・リロード進行度・クールダウンを保持（撃ち切りリロード中にスワップして即応戦、裏でリロード進行といったAC本家の高度なローテーション戦術が可能）
    - 手続き的メカニカル装填・スワップ音（`weapon_swap.wav`: ラッチ解除＋サーボ移動＋ロック打撃音）
    - 機体背部・両肩のウェポンハンガー支持ラックおよび背負い武器の3Dモデル描画（スワップ時に腕と肩の武器モデルが視覚的にもリアルタイムに入れ替わる）
    - 4スロット対応サイバーHUD（デスクトップ ＆ VR立体キャノピーHUDに武器略称・残弾バー・`[Q] SWAP` / `[E] SWAP` ガイドを表示）
14. **多層発光スラスタージェット噴射システム (`src/graphics/D3D11Renderer.cpp`, `assets/shaders/basic.hlsl`)**
    - ブースト状態（通常ホバー巡航、QB、AB、バーニア垂直上昇）に応じたリアルタイム3Dプラズマ炎噴出
    - ノズル基部ディスク＋八角錐マントル＋中心交差ストリークによる高密度ハイブリッド3Dプルームジオメトリ
    - DirectX 11 加算合成ブレンド（`SRC_ALPHA + ONE`）および深度テスト（書き込みOFF）によるまばゆいSF発光表現
    - 2パス多層レンダリング（外周のアウタープラズマと中心の純白超高熱インナーコア）
    - 6基の独立スラスターノズル配置（背部メインスラスター×2、肩部サイドブースター×2、足裏バーニア×2）
    - 状態ごとの動的パラメータ制御：
      - アサルトブースト（AB）: 長大（~7m）の灼熱オレンジ/ゴールドのアフターバーナー炎
      - クイックブースト（QB）: 瞬間インパルス爆発（~5m）と時間減衰パルス
      - 通常巡航ホバー: 速度連動（1.3m〜2.8m）のサイバーシアン炎
      - バーニア垂直上昇: 足裏から下方（-Y方向）へ噴射される強力な浮遊リフトジェット
    - マルチプレイヤー対戦相手機（RemoteMech）のスラスター噴射完全同期＆プレイヤー別テーマカラー発光
15. **多彩な新兵装（全9種） ＆ ロードアウト即時換装システム (`src/combat/WeaponSystem.cpp`, `src/graphics/D3D11Renderer.cpp`, `src/audio/AudioManager.cpp`)**
    - 全9種類の個性的なメカ兵装カタログ：
      1. `RF-024 KINETIC RIFLE` (実弾アサルトライフル / 30発)
      2. `EN-010 BEAM RIFLE` (パルスレーザーライフル / 30発)
      3. `PL-040 HEAVY PLASMA` (重プラズマ砲 / 15発 / 単発250ダメージ)
      4. `MG-014 RAPID BURST` (バーストサブマシンガン / 40発)
      5. `ML-080 VERTI-MISSILE` (4連装垂直誘導マイクロミサイル / 24発 / ホーミング追尾 / 爆発スプラッシュ)
      6. `BZ-033 TITAN BAZOOKA` (重バズーカキャノン / 10発 / 単発520ダメージ / 8.5m大爆発スプラッシュ)
      7. `LB-077 MOONLIGHT BLADE` (高周波レーザーブレード近接格闘 / 単発680ダメージ / 光刃プラズマアーク)
      8. `GT-090 VULCAN GATLING` (6連回転ヘビーガトリング / 200発 / 秒間20発の超連射弾幕)
      9. `SG-020 BREAKER SHOTGUN` (8散弾ペレットショットガン / 16発 / 至近距離520ダメージ)
    - 5種の戦闘スタイルプリセット（数字キー `1`〜`5` / 十字キー左右で戦闘中に瞬時換装）：
      - `[1] STANDARD ASSAULT`（標準アサルト）: ライフル＋ビーム＋バースト＋プラズマ
      - `[2] HEAVY SIEGE`（重火器包囲）: バズーカ＋プラズマ＋誘導ミサイル＋ガトリング
      - `[3] CQC MELEE`（近接格闘突撃）: ショットガン＋レーザーブレード＋誘導ミサイル＋ライフル
      - `[4] BULLET STORM`（弾幕制圧）: ダブルガトリング＋誘導ミサイル＋バーストガン
      - `[5] SWARM ARTILLERY`（遠距離爆撃）: バズーカ＋ショットガン＋ダブルミサイルポッド
    - 誘導ミサイルの垂直射出＋ターゲットへのホーミング旋回飛翔ロジック
    - レーザーブレード斬撃時の加算発光プラズマ光刃アーク描画 ＆ 近接薙ぎ払い判定（クリーブ判定 / 射程~8.8m、至近距離密着必中、多段ヒット防止ガード、敵ノックバック、マルチ同期）
    - 全新兵装の3Dパーツモデル（DirectX 11）＆ 手続き的音響（WAV）の自動生成
    - HUD上部へのロードアウト名表示（`LOADOUT [X]: PRESET_NAME`）

    16. **ガレージ・アセンブル（機体パーツ換装） ＆ 肩ミサイル直接射出・ウェポンハンガー分離 (`src/combat/WeaponSystem.cpp`, `src/graphics/D3D11Renderer.cpp`, `src/core/MechController.cpp`, `src/main.cpp`)**
        - **武器カテゴリの分離アーキテクチャ**:
          - `WeaponCategory::BackOnlyWeapon`: ミサイルポッド（`ML-080 VERTI-MISSILE`）など肩固定の専用大型兵装。腕への装備はできず、肩から直接射出。
          - `WeaponCategory::ArmWeapon`: アサルトライフル、ビーム、バズーカ、ガトリング、プラズマ、ショットガン、ブレード等。腕に装備できるほか、肩ハンガーラックに背負って出撃可能。
        - **肩アクションのスマート制御**:
          - 左肩キー（`Q` / `LB`）および右肩キー（`E` / `RB`）押下時：
            - 肩にミサイル（`BackOnlyWeapon`）を装備している場合：腕とのスワップを行わず、機体上部の肩ミサイルハッチ（`GetLeftBackMuzzlePosition()` / `GetRightBackMuzzlePosition()`）からターゲットへ直接ミサイルを斉射！
            - 肩にミサイル以外の武器（`ArmWeapon`）を装備している場合：AC本家仕様のウェポンハンガーとして、腕と肩の武器を瞬時にスワップ！
        - **HUDの動的ガイド表示**:
          - 肩スロットにミサイル搭載時は `[Q] SHOOT` / `[E] SHOOT`
          - 肩スロットにハンガー武器搭載時は `[Q] SWAP` / `[E] SWAP`
        - **ガレージ・アセンブル画面（Garage / Assemble Mode）**:
          - メインメニューの「`GARAGE / ASSEMBLE`」項目および戦闘中の `G` キーで即座にアクセス可能
          - 背景で格納庫の自機メカが待機し、アセンブルした4スロットの武器3Dモデルがリアルタイムに反映
          - 左パネル: 4スロット（`[1] R-ARM`, `[2] L-ARM`, `[3] R-BACK`, `[4] L-BACK`）の選択とパーツ換装（`W`/`S` または十字キー上下でスロット選択、`A`/`D` または十字キー左右でパーツ循環換装）
            - 腕スロットは肩専用兵装（ミサイル）を自動除外し腕用武器のみ選択可能
            - 肩スロットはミサイル・ハンガー用腕武器の両方全9種から自由に搭載可能
          - 右パネル: 選択パーツの型番（MODEL ID）、カテゴリ、攻撃力（ATK POWER）、装弾数、発射サイクル、リロード時間、初速、特殊効果（誘導ミサイル、爆風スプラッシュ、散弾、高速回転ガトリング、プラズマ光刃）を詳細表示
          - 下部操作ガイド、パーツ換装サウンド（`audioManager.PlayEquipChange()`）、`ENTER` / `SPACE` / `A` で換装機体のまま即座に出撃・テスト戦闘開始

    17. **fastgltf による glTF 2.0 / GLB 3Dモデル読み込み＆レンダリングシステム (`src/graphics/ModelLoader.hpp`, `src/graphics/ModelLoader.cpp`, `src/graphics/D3D11Renderer.cpp`)**
        - **C++20 高速モデルパーサー**:
          - `fastgltf` (v0.8.0) ＋ `simdjson` による超高速 glTF 2.0 Binary (`.glb`) / JSON (`.gltf`) 解析
        - **DirectX 11 メッシュ生成＆キャッシュ (`GLTFModel`, `ModelManager`)**:
          - 頂点座標（POSITION）、法線（NORMAL、未設定時のフラット法線自動生成対応）、頂点カラー（COLOR_0）、マテリアルBaseColorFactor、インデックスをDirectX 11バッファ（VertexBuffer/IndexBuffer）に変換
          - モデルのAABB（最小/最大境界）と中心オフセットを自動計算し、機体マウント位置へ自動センタリング＆スケーリング
          - モデルキャッシュにより、同一兵装・パーツの重複ロードを防止
        - **ハイブリッド・フォールバック描画**:
          - `assets/models/weapons/<WEAPON_ID>.glb`（または `<SHORT_NAME>.glb`）が存在する場合：GLB 3Dモデルを優先描画
          - 存在しない場合：従来のプロシージャルメッシュ（ボックス）で描画され、エラーで落ちない安全設計
        - **全9兵装のリアルタイム3Dモデル配備**:
          - `assets/models/weapons/` 配下に全9兵装（`RF-024`, `EN-010`, `PL-040`, `MG-014`, `ML-080`, `BZ-033`, `LB-077`, `GT-090`, `SG-020`）のGLBファイルを配備完了
    18. **プロシージャル・メカモーション＆姿勢制御システム (`src/core/MechController.cpp`, `src/combat/WeaponSystem.cpp`, `src/graphics/D3D11Renderer.cpp`, `src/main.cpp`)**
        - **コード駆動型リアルタイム・メカモーション**:
          - 3Dモデリングソフトでの手作業ボーンアニメーション作成を一切必要とせず、機体・関節・油圧・武器の挙動を物理演算とC++コードで完全制御
        - **動的リコイル反動（Kickback）＆ スプリング復元**:
          - 武器種別ごとに固有のリコイル強度（バズーカ: 0.48mの大衝撃、ショットガン: 0.38m、プラズマ: 0.28m、ライフル: 0.18m、ガトリング: 0.045mの高速連打）を設定
          - 射撃時に腕モデルが瞬間的に後退し、油圧ダンパースプリングで滑らかに復元
        - **照準エイム仰角（Aim Pitch）サーボ追従**:
          - ロックオン敵やレティクルの3D座標（高低差）に合わせて、両腕の兵装・銃口が上下（-45°〜+45°）に滑らかにサーボ旋回
          - 銃口位置（`GetRightMuzzlePosition()` / `GetLeftMuzzlePosition()`）も仰角に追従し、照準と弾道トレーサーの射出方向が完全に一致
        - **ガトリングガン銃身（バレル）の回転アニメーション**:
          - ガトリング射撃時にバレルが角速度（最大 60 rad/s）まで高速スピン加速
          - トリガーを離すと慣性と摩擦により「キュルルル…」と余力でゆっくり減速して停止
        - **地上ホバー微細浮遊揺らぎ ＆ 着地サスペンションダンパー**:
          - 地上ホバー巡航中に毎秒数サイクルの微細な上下浮遊ボビング運動を生成
          - 高所からの着地（タッチダウン）時に機体全体がグッと沈み込んで衝撃を吸収する油圧サスペンション動作（Landing Dip）
        - **旋回・平行移動時の慣性バンク傾斜**:
          - クイックブースト（QB）や旋回操作時に機体が自然にロール傾斜し、超高速メカならではの重量感・迫力を演出

    19. **機体フレームパーツ（HEAD, CORE, ARMS, LEGS）アセンブルシステム (`src/core/FrameSystem.hpp`, `src/core/FrameSystem.cpp`, `src/core/MechController.cpp`, `src/graphics/D3D11Renderer.cpp`, `src/vr/VRManager.cpp`, `src/main.cpp`)**
        - **全4部位・16種類の機体フレームパーツカタログ**:
          - `HEAD`: `HD-012 RADAR` (標準バランス), `HD-045 SENSOR` (軽量高感度), `HD-090 BULWARK` (重装甲), `HD-077 HUNTER` (格闘特化)
          - `CORE`: `CR-020 STANDARD` (中量高耐久), `CR-055 STREAM` (軽量流線型), `CR-099 FORTRESS` (超重装甲要塞), `CR-080 OVERBOOST` (大出力ジェネレータ)
          - `ARMS`: `AM-030 STRIKER` (標準腕部), `AM-077 VELOX` (高速サーボエイム), `AM-066 COLOSSUS` (超重反動吸収ダンパー), `AM-088 VALIANT` (格闘近接ブースター)
          - `LEGS`: `LG-040 BIPEDAL` (汎用中量2脚), `LG-066 REVERSE-JOINT` (高跳躍逆関節), `LG-088 QUADRUPED` (重装4脚・大積載), `LG-055 RUNNER` (超高速スプリント巡航)
        - **リアルタイム機体性能（TOTAL SPECS）動的再計算**:
          - フレームの組み合わせにより、AP（耐久値）、機体総重量、最大EN容量、通常巡航速度、QB推力、AB突進力、跳躍力、反動抑制力、エイム追従速度が動的に変化
          - `frameSystem.ApplyToMech(mech)` により、換装した瞬間に物理挙動・機体スペックが即座に反映
        - **ガレージ・アセンブル画面のタブ切り替えUI**:
          - `[Q] / [E]` キー（またはゲームパッド `[LB] / [RB]`）で `WEAPONS UNIT` ↔ `FRAME PARTS` をシームレスに切替
          - 左パネル: 各部位の選択（`W`/`S` または上下キー）とパーツ換装（`A`/`D` または左右キー）
          - 右上パネル: 選択パーツの型番、部位、AP、重量、EN負荷、固有機能（姿勢安定、ジェネレータ出力、反動減衰、跳躍推力）の詳細スペック表示
          - 右下パネル: 機体の総合性能サマリー（TOTAL AP、総重量、最大EN、巡航速度、QB推力、AB突進力、跳躍力）をリアルタイム集計表示
        - **3Dフレームパーツのハイブリッド描画 ＆ VRコックピット最適化**:
          - 部位ごとに独立した3Dメッシュレンダリング（GLBモデルが存在する場合は優先読み込み、未配置時は専用プロシージャルメッシュで描画）
          - FPV（VR一人称視点）時は視界を遮らないよう頭部・胴体を非表示にし、腕部フレームのみをコックピット内に描画
          - OpenXR PCVR（Quest 3S等）での両眼立体レンダリングにも完全対応

    20. **内装パーツ（BOOSTER, FCS, GENERATOR）アセンブルシステム (`src/core/InternalSystem.hpp`, `src/core/InternalSystem.cpp`, `src/combat/TargetLockSystem.hpp`, `src/core/MechController.cpp`, `src/graphics/D3D11Renderer.cpp`, `src/main.cpp`)**
        - **3系統・12種類の内装パーツカタログ**:
          - `BOOSTER`:
            - `BST-010 ALULA`: 軽量・高QB特化（超高推力 78m/s、高燃費、スプリント機動）
            - `BST-020 GILLS`: 低消費・高効率汎用型（QB消費 170EN の驚異的な低燃費、継続戦闘向き）
            - `BST-030 FLUEGEL`: 垂直跳躍・空中戦特化（圧倒的な上昇力 34、滞空爆撃向け）
            - `BST-040 BXT-7`: 超重推力型（重装甲機を力強く推進する大出力メインスラスター）
          - `FCS (Fire Control System)`:
            - `FCS-010 OCELLUS`: 至近距離インファイト特化（近距離追従 2.2x、ショットガン/格闘ブレード必中）
            - `FCS-020 TALBOT`: 近中距離バランス万能型（近1.5x、中1.6x、ライフル/パルス主眼）
            - `FCS-030 WUERGER`: 遠距離狙撃＆ミサイル特化（遠距離追従 2.1x、ミサイル即時ロック 0.65x）
            - `FCS-040 ABBOT`: 高機動CQC速射型（近1.8x、中1.4x、マシンガン弾幕制圧）
          - `GENERATOR`:
            - `GEN-010 MING-TANG`: 安定循環型（中容量 1200EN、超高速リチャージ 620EN/s、短ディレイ）
            - `GEN-020 SAN-TAI`: 超大容量型（最大級の 1850EN プール、連続QB・長距離滞空対応）
            - `GEN-030 HOKUSHI`: 大出力バランス型（1450EN、安定したEN循環供給）
            - `GEN-040 CORAL-RED`: 特殊プラズマ大出力型（1600EN、使い切り後の瞬間復帰 0.18s）
        - **FCS距離アシスト適性（近・中・遠距離）によるリアルタイム照準収束演算**:
          - AC6本家基準の交戦距離区分（近距離 <130m / 中距離 130〜260m / 遠距離 >260m）
          - 装備したFCSの距離適性倍率と腕部フレームのサーボ追従速度（`aimSpeed`）を掛け合わせ、インナー赤照準の追従・未来位置収束速度をミリ秒単位で動的計算
          - ミサイルロックオン時間（短縮/延長）の動的補正
        - **ガレージ・アセンブル画面の3タブ統合UI**:
          - `[Q] / [E]`（または `[LB] / [RB]`）で `WEAPONS UNIT` ↔ `FRAME PARTS` ↔ `INTERNAL PARTS` を循環切替
          - 左パネル: 3スロット（`BOOSTER`, `FCS`, `GENERATOR`）の選択とパーツ循環換装
          - 右上パネル: 内装固有パラメータ（推力、QB消費EN、3区分FCS適性、ミサイル補正、EN容量、秒間回復量、復旧遅延）の詳細表示
          - 右下パネル: 機体総重量・ブースター推力・QB消費量・EN回復速度などを総合集計した「AC TOTAL PERFORMANCE SUMMARY」をリアルタイム表示

    21. **戦術戦闘マップ（Tactical Complex Arena）＆ 屋根付きシェルター・立体構造物 (`src/graphics/GridFloor.cpp`, `src/main.cpp`)**
        - **本格的な立体戦術マップへの全面刷新**:
          - 従来の平坦なトレーニング場から、アーマードコア（AC6）のテストグラウンドや対戦アリーナに匹敵する高密度な立体交戦マップへ再構築
        - **屋根付きシェルター（防空シェルター × 2基）**:
          - 南西シェルター (`-18, 8.5, -15`、屋根 26m × 1.2m × 24m) ＆ 北東ガントリーシェルター (`22, 9.5, 28`、屋根 28m × 1.2m × 22m)
          - メカ機体（全高約4m）が余裕を持ってブースト滑走でくぐり抜けられる天井高（クリアランス 7.9m〜8.9m）
          - 上空からの垂直誘導ミサイル、迫撃砲、上空ドローンからの射撃を完全にシャットアウトする防護天板
          - 屋根の上にはブースト跳躍で登ることができ、上空見渡し・対空射撃用プラットフォームとしても機能
        - **少し大きな箱・大型施設ブロック（Mega Blocks & Containers）**:
          - 中央東メガカーゴブロック (`38, 5.5, -12`、18m × 11m × 22m、上面高 11m)
          - 北西研究施設ブロック (`-42, 6.5, 32`、20m × 13m × 18m、上面高 13m)
          - 南西2段積みコンテナ (`-28, 2.5, -38` & `-28, 7.5, -38`、高さ10mの階段状ジャンプ台)
          - 南東カーゴバンカー (`45, 3.5, -45`、14m × 7m × 14m)
        - **戦術遮蔽壁（Tactical Cover Walls）**:
          - 中央西 L字型防壁 (`(-5, 3, 8)` & `(3, 3, -0.5)`、高さ6m): コーナーを使った射線切り・飛び出し撃ち（ピークショット）の起点
          - 北側トレンチ壁 (`-10, 3, 55`)、東側防壁 (`16, 2.5, -36`)、南側低防壁 (`-2, 2, -55`)、中北防壁 (`2, 2.5, 24`)
        - **巨大支柱（Giant Pillars）**:
          - 高さ28m〜32mの大支柱4本 (`(-12, 14, 26)`, `(14, 14, 2)`, `(-48, 16, -6)`, `(52, 16, 18)`): 高速旋回スラロームや射線切りに最適
        - **スロープ＆連結高台デッキ**:
          - 西側傾斜スロープ (`-55, 3.25, 24`) ＋ 高さ7mの連結高台デッキ (`-55, 3.5, 44`)
        - **DirectX 11 描画 ＆ Jolt Physics 静的コライダーの完全同期**:
          - 屋根天板・4隅支柱・メガブロック・防壁・支柱・スロープの全構造物が物理判定と描画で1ミリの狂いもなく完全一致
          - レイキャスト着弾、バズーカ爆風スプラッシュ、ホーミングミサイル着弾、機体接触判定に対応
        - **新マップ対応のダミーターゲット 4体配置**:
          - 防壁裏の地上標的（`Target-01 Cover`）、シェルター屋根上の高所標的（`Target-02 Shelter Roof`）、メガブロック上の高耐久要塞標的（`Target-03 Mega Block`）、支柱間を浮遊する対空標的（`Target-04 Aerial`）

    22. **被弾方向インジケーター ＆ タクティカルミニレーダー ＆ マルチ同期消滅バグ完全根絶 (`src/core/MechController.cpp`, `src/network/NetworkManager.cpp`, `src/graphics/D3D11Renderer.cpp`)**
        - **被弾方向インジケーター（Damage Direction Indicator / Hit Arc HUD）**:
          - 被弾した瞬間に、攻撃元の方向（自機のYaw角度との相対方位）を自動計算し、画面中央レティクル周囲に**赤い扇状アーク（湾曲した警告インジケーター ＋ 先端ポインター矢印）**を動的描画（0.95秒フェードアウト）。
          - 背後、真横、高台、遮蔽物の裏など、どこから撃たれたかが瞬時に直感把握可能。VR立体キャノピーHUDにも完全対応。
        - **AC6風 タクティカルミニレーダー（Tactical Mini-Radar HUD）**:
          - 画面右上に半径140mの全周囲コックピット走査ミニレーダーを展開（二重距離リング、十字軸、回転スイーパ線）。
          - 自機を中心として周囲の敵機（RemoteMech）や標的（TargetDummy）をプレイヤー別カラーで常時プロット。
          - 高低差アイコン（自機より上空なら上三角、足元・地下なら下三角、同高度なら菱形、撃破中ならバツ印）により、シェルター屋根の上やコンテナ裏に隠れた敵も一目瞭然。
        - **画面外敵インジケーター（Off-Screen Edge Arrow Indicator）**:
          - 敵機がカメラの視野角（画面外）から外れた場合、画面の端（スクリーンエッジ）に敵の方向を指し示すカラーChevron矢印（`>`）と距離計（`[ 85M ]`）を表示。
          - 視界外へ回り込まれた際も、どちらへカメラを旋回すれば視界に捉えられるかが直感的に判明。
        - **マルチプレイヤー接続時「地下100m潜り消滅バグ」の完全根絶**:
          - 接続成立時に最初のパケット受信前は地下100mに配置され、最初のパケット受信後も線形補間（Lerp）で数秒かけて地上へ浮上してくるため「友達が消えていて、少し時間が経つと現れる」直接原因となっていたバグを特定・修正。
          - 接続時の正規スポーン位置事前初期化 ＆ 初回パケット受信時の絶対スナップにより、遅延出現・消滅現象を完全に解消。
        - **撃破・リスポーン同期改善 ＆ 頭上「`[ DESTROYED ]`」マーカー**:
          - 撃破された相手の頭上に「`[ DESTROYED ]`」マーカーを表示し、消滅と誤認しないUIフィードバックを確立。
          23. **ゲームパッド右スティックによるAC6風ターゲットフリック切り替え ＆ デバウンス制御 (`src/combat/TargetLockSystem.cpp`, `src/combat/TargetLockSystem.hpp`, `src/core/MechController.hpp`, `src/main.cpp`)**
        - **コントローラーでターゲット切り替えができなかった根本原因の解決**:
          - 従来のフリック判定がマウス移動量（`mouseDeltaLen > 0.040f`）のみに依存しており、右スティックの最大入力値（`0.035f`）では物理的にしきい値を超えられなかった不具合を特定・抜本改修。
        - **エッジトリガー式スティックフリック検出 ＆ デバウンス制御**:
          - `MechInputState` に右スティックの生のチルト量（`lookStickX`, `lookStickY`）を直接保持。
          - スティックを一定以上倒した瞬間（`stickMag > 0.45f`）のみを検知するエッジトリガー（`m_stickFlickTriggered`）とクールダウンタイマー（0.28秒）を実装。
          - スティックを倒しっぱなしにした際にターゲットが高速で入れ替わり続ける暴走を完全に防止し、スティックをニュートラル（`< 0.25f`）に戻すことで次のフリック受付が可能。
        - **AC6本家仕様の直感的な方向優先スイッチング**:
          - スティックを倒した方向（ベクトル）にある敵機体を優先してスコアリング・選択。
          - ターゲット切り替え成功時にはメカニカルなロックオン音（`audioManager.PlayLockOn()`）が鳴り、明確な操作フィードバックを提供。
          - 倒した方角に敵がいない場合にハードロックが勝手に解除される誤作動を排除し、快適なターゲットスイッチングを実現。

    24. **スタッガー / ACS（姿勢制御）システム ＆ ダイレクトヒット直撃補正 (`src/core/MechController.cpp`, `src/combat/WeaponSystem.cpp`, `src/combat/TargetDummy.cpp`, `src/core/FrameSystem.cpp`, `src/network/NetworkManager.cpp`, `src/audio/AudioManager.cpp`, `src/graphics/D3D11Renderer.cpp`)**
        - **AC6本家仕様のスタッガー / ACS過負荷システム**:
          - 機体（自機、敵プレイヤー、TargetDummy）がAP（耐久値）とは別に**ACSゲージ（姿勢制御負荷）**を保持。
          - 攻撃被弾により武器ごとの衝撃値（`impact`）が蓄積し、ACS限界に達すると**「STAGGERED（機能停止・過負荷硬直）」**状態へ移行。
          - スタッガー中は約2.4秒間、ブースト・移動操作が無効化（強制失速・急激な摩擦停止）され、自機操縦系が完全麻痺。
          - スタッガー状態中の被弾はすべて**「DIRECT HIT（直撃特大ダメージ）」**となり、武器固有の直撃倍率（`directHitMult`: 1.5x〜2.5x）が適用され大ダメージが発生。
          - 被弾が途切れて約2.2秒経過するとACSが徐々に自然冷却（ACS Discharge）。
        - **全9兵装の衝撃力（Impact）＆ 直撃倍率（Direct Hit Mult）**:
          - `RF-024 KINETIC RIFLE`: 衝撃 75 / 直撃 1.7x
          - `EN-010 BEAM RIFLE`: 衝撃 55 / 直撃 1.5x
          - `PL-040 HEAVY PLASMA`: 衝撃 310 / 直撃 1.9x
          - `MG-014 RAPID BURST`: 衝撃 42 / 直撃 1.6x
          - `ML-080 VERTI-MISSILE`: 衝撃 260 / 直撃 1.8x
          - `BZ-033 TITAN BAZOOKA`: 衝撃 520 / 直撃 2.2x（一撃で大量のACS負荷を蓄積）
          - `LB-077 MOONLIGHT BLADE`: 衝撃 580 / 直撃 2.5x（スタッガー直撃時に超絶大ダメージ）
          - `GT-090 VULCAN GATLING`: 衝撃 28 / 直撃 1.55x（連射で瞬く間にACS負荷を蓄積）
          - `SG-020 BREAKER SHOTGUN`: 衝撃 480 / 直撃 2.05x（至近距離での即スタッガー誘発）
        - **機体フレーム（HEAD, CORE, ARMS, LEGS）による姿勢安定度アセンブル**:
          - 全16種類のフレームパーツに姿勢安定性能（`attitudeStability`）を設定。
          - パーツ構成により最大ACS許容量が動的に再計算（軽量機: ~860 ACS、標準機: ~1200 ACS、重装甲機: ~1860 ACS）。
          - ガレージ画面の右パネルおよび総合サマリー（`AC TOTAL PERFORMANCE SUMMARY`）に `ATTITUDE STABILITY (ACS)` を表示。
        - **重厚な音響演出 ＆ 危機警報システム (miniaudio)**:
          - `stagger_break.wav`: 重金属破砕音＋電子放電ノイズによるスタッガー発生の破砕サウンド。
          - `stagger_alarm.wav`: 自機スタッガー時のコックピット緊急危機警報ビープ。
          - `system_restored.wav`: スタッガー解除時のSFシステム復帰チャイム。
        - **2D / 3D / VR立体キャノピーHUDの完全表示**:
          - 自機HUD: 左下にAPバーと並ぶ琥珀色/赤のACSゲージバー、`ACS xxx/xxx` 数値テキスト、自機スタッガー時の赤点滅警告 `! STAGGER OVERLOAD !`。
          - 標的・敵機頭上HUD: APバーの直下にACSバー、スタッガー中の赤点滅インジケーター `! STAGGERED !`。
          - VRキャノピーHUD: コックピット前面ホログラムに立体ACSバーおよび `! STAGGER OVERLOAD !` 警告投影。
        - **マルチプレイヤー4人対戦パケット完全同期**:
          - `HitEventPacket` に `impact` と `directHitMult` を統合。
          - `MechStatePacket` の flags にスタッガーフラグ（`0x10`）を同期し、全クライアント間で相手機体のスタッガー硬直・直撃補正を完全連動。

    25. **アサルトブースト解除 ＆ ブーストキック（Assault Boost Kick）システム (`src/core/MechController.cpp`, `src/combat/WeaponSystem.cpp`, `src/core/FrameSystem.cpp`, `src/network/NetworkManager.cpp`, `src/audio/AudioManager.cpp`, `src/graphics/D3D11Renderer.cpp`, `src/main.cpp`)**
        - **アサルトブースト（AB）即時解除 / エアブレーキ減速**:
          - AB突進中に後退操作（キーボード `S` キー / ゲームパッド左スティック下倒し `moveForward < -0.3f`）を行うことで、ABを即座にキャンセル可能。
          - 強力な逆噴射エアブレーキ減速が作用し、立ち回りや交戦距離の微調整が自在に。
        - **ブーストキック（Boost Kick）発動メカニクス**:
          - AB突進中にもう一度ABキー（キーボード `Ctrl` / ゲームパッド `L3`）を押すことで、超高速ロケット突進格闘**「ブーストキック」**が発動。
          - EN 200を消費し、約0.42秒間にわたってABをも上回る超高速（~85〜100m/s）でターゲットめがけてロケット突進。
          - クイックブースト（QB）入力や敵からのスタッガー硬直で安全にキャンセル可能。
        - **特大ダメージ ＆ ACS衝撃 ＆ ノックバック物理判定**:
          - 自機前面（前方 2.2m、半径 2.4m の円柱空間）で標的（`TargetDummy`）および敵機体（`RemoteMech`）に対する近接衝突判定を実行。
          - 1回の突進で1ヒットのみ適用される重複ヒット防止ガード（`m_boostKickHit`）。
          - 直撃時に特大近接ダメージ（520〜750）＋ 絶大なACS衝撃（740〜940）を叩き込み、相手を一撃でスタッガー寸前または即スタッガーに追い込む。
          - すでにスタッガー状態の相手に対してはダイレクトヒット直撃倍率（2.0x）が適用され、1000〜1500の壊滅的特大ダメージ。
          - 命中した敵機体（および標的）をキック進行方向へ強烈に弾き飛ばす後方ノックバック物理（`ApplyKnockback`）。
        - **脚部フレーム（FrameSystem）によるキック性能アセンブル**:
          - `LG-066 REVERSE-JOINT`（逆関節）: スプリング跳躍力を活かした高衝撃キック（衝撃 940 ACS / 威力 680）
          - `LG-088 QUADRUPED`（4脚）: 重装甲質量をぶつけるメガトンキック（威力 750 / 衝撃 880 ACS）
          - `LG-040 BIPEDAL`（中量2脚）: バランス標準キック（威力 520 / 衝撃 740 ACS）
          - `LG-055 RUNNER`（スプリント）: 高速軽量キック（威力 480 / 衝撃 680 ACS）
          - 機体の総重量に応じた運動エネルギー補正（重量比 0.85x〜1.30x）。
        - **大迫力の音響演出 (miniaudio)**:
          - `boost_kick_thrust.wav`: ロケット急加速時の超大推力バーニア放電＆轟音。
          - `boost_kick_hit.wav`: メガトン打撃着弾音＋重金属粉砕・軋み＋電子クラッシュ音。
        - **プロシージャル・キックポーズ ＆ 超長大スラスター炎 ＆ HUDガイド**:
          - キック突進時、機体の脚部フレーム（Legs）が前方斜め上へダイナミックに突き出される専用プロシージャル・キック姿勢をリアルタイム生成。
          - 背部・肩部・足裏から通常AB（6.8m）を超える超長大（~8.5m）の灼熱オレンジ/クリムゾンアフターバーナー炎が噴出。
          - 2D HUD / VR立体キャノピーHUDにAB中の操作ガイド（`[CTRL / L3] BOOST KICK    [S] CANCEL AB`）およびキック中の赤点滅インジケーター（`>> BOOST KICK <<`）を表示。
        - **マルチプレイヤー4人対戦パケット完全同期**:
          - `MechStatePacket` の flags にブーストキックフラグ（`0x20`）を追加し、リモート機体のキック突進姿勢・スラスター炎・被弾ノックバックを全クライアント間で完全同期。

    26. **アセンブル永続化（loadout.ini）＆ ガレージ専用クリーン展示背景 ＆ 数字キー武器切替の廃止 (`src/core/LoadoutManager.hpp`, `src/core/LoadoutManager.cpp`, `src/graphics/GridFloor.cpp`, `src/graphics/D3D11Renderer.cpp`, `src/main.cpp`)**
        - **アセンブル機体状態の永続保存・自動復元（`LoadoutManager`）**:
          - 機体の武器4スロット（`RightArm`, `LeftArm`, `RightBack`, `LeftBack`）、フレームパーツ4部位（`Head`, `Core`, `Arms`, `Legs`）、内装パーツ3系統（`Booster`, `FCS`, `Generator`）の全構成を `loadout.ini` に自動保存・復元。
          - ガレージ画面でパーツを変更するたびにリアルタイムでオートセーブ。出撃時、メインメニュー戻り時、ゲーム終了時にも確実に保存。
          - 次回ゲーム起動時に `loadout.ini` を自動読み込みし、前回カスタマイズしたアセンブル状態のまま即座に戦場へ出撃可能。
          - ガレージ画面下部に緑色の `[AUTO-SAVED: loadout.ini]` インジケーターを表示し、保存状況を視覚的に通知。
        - **アセンブル画面で機体が壁に埋まる問題の完全解消（クリーン展示空間）**:
          - ガレージ（およびメインメニュー）の3D背景描画で、アリーナの戦闘用遮蔽壁やブロックを除外し、平坦でクリアなサイバーグリッド床のみを描画する `GridFloor::RenderFloorOnly` を実装。
          - マップ中央のL字型防壁の座標を東側へオフセットし、アリーナ原点 `(0, 0, 0)` 付近の中央コリドー通路を完全にクリア化。
          - ガレージに入った瞬間に自機をガレージ展示台（原点 `0.0, 1.2, 0.0`）に直立配置し、カメラが機体を中心として半径5.6mで360°優雅に旋回。壁や構造物に埋まる事故を100%永久に防止。
          - 戦闘中に `G` キーでガレージに入った場合、直前の戦闘位置・回転を記憶し、出撃時にその位置へスムーズに復帰。
        - **数字キー（1〜5）およびコントローラーによる武器プリセット切替の完全廃止**:
          - 戦闘中の意図しない武器リセットを防ぐため、キーボードの数字キー（1〜5 / テンキー1〜5）およびゲームパッド十字キー左右でのプリセット切替処理を完全削除。
          - HUD左上の案内テキストを `CUSTOM ASSEMBLE UNIT  [G] GARAGE` に変更し、アセンブル機体としていつでもガレージへアクセス可能であることを明示。

    27. **自律敵戦闘AIボット（AIBotMech）＆ 3種アリーナ環境モード切替システム (`src/combat/AIBotMech.hpp`, `src/combat/AIBotMech.cpp`, `src/combat/WeaponSystem.hpp`, `src/combat/WeaponSystem.cpp`, `src/combat/TargetLockSystem.hpp`, `src/combat/TargetLockSystem.cpp`, `src/network/NetworkManager.hpp`, `src/network/NetworkManager.cpp`, `src/graphics/D3D11Renderer.hpp`, `src/graphics/D3D11Renderer.cpp`, `src/vr/VRManager.hpp`, `src/vr/VRManager.cpp`, `src/main.cpp`)**
        - **自律敵戦闘AIボット（`AIBotMech`）**:
          - 標的ダミーとは異なり、プレイヤーと同様にアリーナ内を自律機動・跳躍・旋回・戦闘する本格的な敵戦闘メカAI。
          - **戦術的マニューバ＆AIステート制御**:
            - 遠距離（>100m）: アサルトブースト（AB）による高速間合い詰め
            - 中距離（30〜85m）: 横ステップ・旋回ストレイフ機動を行いながら左右腕ライフルの予測射撃および肩部誘導ミサイルの斉射
            - 至近距離（<28m）: 突進ブーストキックを繰り出して自機を吹き飛ばす、またはクイックブースト（QB）による緊急後方・左右ステップ離脱
          - **予測エイム（Lead Targeting）**:
            - ターゲットの現在速度ベクトルを考慮した未来着弾位置を動的に算出し、腕部・肩部武装の照準を偏差射撃。
          - **双方向ダメージ＆物理・ACSスタッガー判定**:
            - ボットが発射した弾丸（実弾・ビーム・誘導ミサイル）が自機メカに命中し、AP減少およびACS負荷が蓄積。
            - 自機の射撃・近接ブレード・ブーストキックがボットのJolt Physicsコライダーにリアルタイム着弾。
            - 衝撃蓄積によるスタッガー硬直、赤点滅、ダイレクトヒット特大ダメージ、HP0での爆発四散、リスポーンタイマーを完全実装。
          - **多層発光スラスター＆3D描画演出**:
            - 状態連動のリアルタイム3Dプラズマ炎噴射（通常巡航、AB長大アフターバーナー、QB瞬間パルス）。
            - 専用のダーククリムゾン/チャコール外装カラーリング、ライフル・ミサイルポッドの3Dメッシュ描画。
        - **3種のアリーナ環境モード（`ArenaMode`）**:
          1. `Clean / Empty`: 的もボットも一切存在しない、純粋な対戦・機動テスト用フラットアリーナ。
          2. `Target Dummies`: 練習用標的ダミー4体（地上遮蔽、屋根上、メガブロック要塞、空中浮遊）が配置された射撃テストアリーナ。
          3. `Combat Bots`: 自律行動する戦闘AIボット2機（`BOT-ALPHA`, `BOT-BETA`）がスポーンし、実戦の激闘を繰り広げる戦闘アリーナ。
        - **シームレスな切替操作 ＆ サイバー通知バナー**:
          - メインメニューでの直接出撃（`ARENA (BOT COMBAT)`, `ARENA (TARGET DUMMIES)`, `ARENA (CLEAN / EMPTY)`）。
          - 戦闘中にキーボードの `M` キーまたはゲームパッド十字キー上（`DPAD_UP`）を押すことで、3つのモードをその場で即座に循環トグル切替。
          - モード切替時に画面中央上部へ琥珀色/シアンのサイバー通知バナー（`>> ARENA MODE: COMBAT BOTS <<` 等）をアニメーション表示。
        - **マルチプレイヤー ＆ FCS ＆ HUD ＆ VR完全統合**:
          - `ArenaModePacket` によるマルチプレイヤー4人対戦時のホスト・クライアント間アリーナモード完全同期。
          - FCSターゲットロックオンシステム（中クリック/R3/Fキー、右スティックフリック切替、インナーレティクル追従）にAIボットを完全統合。
          - AIボット頭上への3Dホログラフィック照準枠、APバー、ACSバー、スタッガー警告、`BOT 1`〜`BOT 2` ネーム表示、DESTROYED表示。
          - ミニレーダーへの高低差付き敵プロット、画面外Chevron矢印（`>`）インジケーター対応。
          - OpenXR PCVR（Meta Quest 3S等）での両眼立体レンダリングおよびキャノピーHUDへの完全連動。

    26. **ゲーム内完結 Tailscale マルチプレイ ＆ DIRECT IP CONNECT 画面 (`src/network/NetworkManager.cpp`, `src/graphics/D3D11Renderer.cpp`, `src/vr/VRManager.cpp`, `src/main.cpp`)**
        - **外部バッチや設定ファイル手動編集の完全撤廃**:
          - 従来の黒いコマンドプロンプトや `network.ini` の手動書き換えを一切排除し、DirectX 11のサイバーUIゲーム画面内だけで接続・ホストが完結する完全スタンドアローンフローを実現。
        - **Tailscale IPv4 自動検出 ＆ ワンキークリップボード連携**:
          - WindowsネットワークAPI（`GetAdaptersAddresses`）により、PC上の Tailscale 仮想アダプター（`100.x.x.x`）やローカルLAN IPを瞬時に自動検出。
          - ホスト待機中や画面上に「`YOUR TAILSCALE IP: 100.xx.xx.xx:7777`」を分かりやすく表示。
          - ホスト中または接続画面で `[C]` キー（またはゲームパッド `[Y]`）を押すだけで、自分のTailscale IPが即座にクリップボードへコピーされ、友達にチャット等でそのまま貼り付けて教えることが可能。
        - **DIRECT IP CONNECT 画面（ゲーム内IP入力・接続UI）**:
          - メインメニューの `[MULTIPLAYER (CONNECT)]` 選択時に、ゲーム内専用GUI画面へシームレス遷移。
          - **ワンタッチペースト**: 友達から送られてきたIPを **`Ctrl + V`**（またはゲームパッド `[X]`）で一発貼り付け可能（数字とドットのみを自動サニタイズ）。
          - **直接タイピング**: テンキーおよび数字キー、ドット、Backspaceによる直感的な直接編集と点滅カーソル表示。
          - **自動保存・次回ワンクリック接続**: 入力したIPは `network.ini` へ自動保存され、次回起動時も前回の接続先が維持されるため `Enter`（またはゲームパッド `[A]`）を押すだけで即接続可能。
        - **VR（OpenXR）完全対応**:
          - コックピット前面2.0mの3Dホログラム空間に接続UIを立体投影し、VR HMD（Meta Quest 3S等）を装着したままでも完全操作可能。

---

## 3. 次のロードマップ
- PBRテクスチャ（BaseColor, NormalMap, MetallicRoughness）のシェーダーサンプリング対応
- 機体カラーリング（プライマリ/セカンダリ/フレーム色）のカスタマイズ機能
- ロビー機能および観戦モード対応

