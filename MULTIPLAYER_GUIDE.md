# Overdrive Core - 4人対戦マルチプレイヤー（バトルロイヤル）対戦ガイド

DirectX 11 + WinSock2 による **最大4人同時対戦（Free-For-All バトルロイヤル）** の接続ガイドです。
ホスト（あなた）を中心とする高速UDPスター型同期により、最大4機のメカが相互にリアルタイムで射撃・被弾・撃破し合えます。

---

## 🎨 4機体の識別カラー ＆ 初期スポーン位置
| プレイヤー | カラーリング | スポーン位置 | 方角 |
| :--- | :--- | :--- | :--- |
| **Player 1 (ホスト)** | **クリムゾンレッド ＆ ダークチタン** | `(0, 2, -40)` | 南側（北を向く） |
| **Player 2 (クライアント 1)** | **コバルトブルー ＆ シルバーホワイト** | `(0, 2, +40)` | 北側（南を向く） |
| **Player 3 (クライアント 2)** | **アンバーゴールド ＆ チャコール** | `(+40, 2, 0)` | 東側（西を向く） |
| **Player 4 (クライアント 3)** | **エメラルドグリーン ＆ ダークバイオレット** | `(-40, 2, 0)` | 西側（東を向く） |

---

## 🚀 友達と遊ぶための接続手順

### 方法1: 設定ファイル `network.ini` を使う（一番おすすめ）
ゲームフォルダ（または `build` フォルダ）内の `network.ini` をメモ帳等で編集します。

- **ホスト側（あなた）**:
  ```ini
  mode = host
  port = 7777
  ```
- **クライアント側（友達1〜3）**:
  ```ini
  mode = client
  ip = <ホストのIPアドレス>
  port = 7777
  ```
あとはお互いに `OverdriveCore.exe` を起動するだけで自動接続され、参加順に Player 2 / 3 / 4 のスロットと専用カラー・位置が自動割り当てされます！

---

### 方法2: ゲーム起動後にキーボードで接続する
1. ホストがゲーム内で **`H` キー** を押す → ホスト待機開始（Player 1: 南側スポーン）
2. 友達がゲーム内で **`C` キー** を押す → ホストへ順次接続（Player 2〜4 として各方角へ自動スポーン）
3. 画面上部に **`[NET: ONLINE (4-PLAYER BATTLE - 4/4 MECHS)]`** など接続人数がリアルタイム表示されます！

---

### 方法3: 起動引数を使う
- ホスト: `.\OverdriveCore.exe --host`
- ゲスト: `.\OverdriveCore.exe --connect <ホストのIP>`

---

## 🌐 インターネット経由（離れた友達）で接続する場合
- **Radmin VPN** または **Tailscale**（無料・登録簡単・ポート開放不要）を利用し、同じ仮想ネットワークに入って表示されたホストの仮想IPを指定するのが一番安全かつ手軽でおすすめです。
- 通常のインターネット回線で行う場合は、ルーター設定で `UDP 7777` をホストPC向けにポートフォワーディング（開放）してください。

---

## 🎮 バトル操作一覧
| アクション | キーボード ＆ マウス | ゲームパッド |
| :--- | :--- | :--- |
| **右腕ライフル射撃** | 右クリック（連射） | RT トリガー |
| **左腕ライフル射撃** | 左クリック（連射） | LT トリガー |
| **リロード (手動装填)** | `R` キー | 十字下キー |
| **FCS ロックオン (ターゲットアシスト)** | 中クリック / `F` キー | R3（右スティック押し込み） |
| **視線フリック / ターゲット切替** | マウスを素早くスワイプ | 右スティックを素早く倒す |
| **巡航ブースト切替** | `Tab` キー | `B` ボタン |
| **クイックブースト (QB)** | `Shift` キー | `X` ボタン |
| **アサルトブースト (AB)** | `Ctrl` キー | `L3`（左スティック押し込み） |
| **上昇バーニア** | `Space` キー | `A` ボタン |
| **カメラ切替 (コックピット ↔ 三人称)** | `V` キー | Back ボタン |
| **ホスト開始 / クライアント接続** | `H` キー / `C` キー | - |

